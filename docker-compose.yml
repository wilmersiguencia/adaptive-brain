services:

  backend:
    build:
      context: ./backend

    container_name: cerebro_backend

    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env

    volumes:
      - ./backend:/app

    restart: always

    networks:
      - cerebro_network


networks:
  cerebro_network:
    external: true